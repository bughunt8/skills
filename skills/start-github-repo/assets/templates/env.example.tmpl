# Committed template. Copy to `.env` and fill in. `.env` is gitignored — never commit it.
# Values here must always be empty. Document the purpose, not the secret.

# --- Runtime ---------------------------------------------------------------
NODE_ENV=development

# --- Application -----------------------------------------------------------
# Public base URL the app is served from. Used for absolute links in emails.
APP_BASE_URL=

# --- Third-party services --------------------------------------------------
# TODO: add one line per variable, with a comment naming what it is for and
# where to obtain it. Anything secret belongs in `.dev.vars` for local
# Cloudflare development, or a repository/environment secret in CI.

# --- Notes -----------------------------------------------------------------
# Only prefixed variables reach a client bundle (VITE_ / NEXT_PUBLIC_), and
# those values are inlined at build time and frozen — dynamic lookups fail.
# `.env.local` is skipped when NODE_ENV=test.
