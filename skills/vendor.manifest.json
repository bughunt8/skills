{
  "schema": 2,
  "$comment": "Single source of truth for every directory in this repository that is vendored from an upstream project. scripts/sync_vendor.py reads this file to perform both the initial import and every scheduled refresh, so an import and an update take exactly the same code path. Do not hand-edit vendored directories: change the manifest, then re-run the script. A source maps a list of upstream subtrees into its own vendored root, preserving the relative distance between them so links inside the vendored content keep resolving. kind=skill-collection tracks each child directory individually; kind=support copies a linked-into subtree whole.",
  "sources": [
    {
      "id": "pstack",
      "name": "pstack",
      "repo": "https://github.com/backnotprop/pstack",
      "homepage": "https://github.com/backnotprop/pstack",
      "upstream_of_upstream": "https://github.com/cursor/plugins/tree/main/pstack",
      "author": "Lauren Tan (@backnotprop, \"poteto\")",
      "license": "MIT",
      "license_path": "LICENSE",
      "ref": "main",
      "pinned_commit": "18e0e908a13553b0e58d065ab26dbc9a972ec8ba",
      "pinned_at": "2026-08-25",
      "dest": "skills/pstack",
      "paths": [
        {
          "from": "skills",
          "to": "",
          "kind": "skill-collection",
          "include": [
            "*"
          ],
          "exclude": [
            "tdd",
            "teach",
            "reflect",
            "bro"
          ],
          "exclude_reasons": {
            "tdd": "Frontmatter name collides with the local skills/engineering/tdd. The local skill is the canonical one for this repository.",
            "teach": "Frontmatter name collides with the local skills/productivity/teach.",
            "reflect": "Frontmatter name collides with the local skills/productivity/reflect/skills/reflect.",
            "bro": "Requires the external backnotprop/bro repository at runtime. Out of scope for an offline-first skills library; add it as its own manifest source if that dependency is ever accepted."
          }
        }
      ],
      "mandatory": [
        "unslop"
      ],
      "mandatory_reason": "unslop is an always-apply writing-quality skill and is a required import for this repository. The sync script fails if a mandatory skill is missing from the upstream tree or has been excluded.",
      "limitations": [
        "poteto-mode, setup-pstack and arena assume a Cursor-style multi-model runner. The engineering discipline in them is portable; the model names (sol, grok, fable, opus 5) are not.",
        "Upstream names the poteto-mode skill `Poteto Mode`, which fails this repository's SK004 and SK005 metadata rules. It is reported as a warning rather than corrected, because correcting it would fork the file."
      ],
      "notes": "Vendored verbatim under a dedicated namespace so upstream refreshes are a mechanical diff. Everything upstream keeps under skills/ is self-contained, so this source needs only one mapped path."
    },
    {
      "id": "job-hunt",
      "name": "job-hunt-skills",
      "repo": "https://github.com/Remotivated/job-hunt-skills",
      "homepage": "https://github.com/Remotivated/job-hunt-skills#readme",
      "author": "Remotivated",
      "license": "MIT",
      "license_path": "LICENSE",
      "ref": "main",
      "pinned_commit": "5fb238febf14a0b41fb5a8fe7384594ad1f59335",
      "pinned_at": "2026-08-25",
      "dest": "skills/job-hunt",
      "paths": [
        {
          "from": "skills",
          "to": "skills",
          "kind": "skill-collection",
          "include": [
            "*"
          ],
          "exclude": [],
          "exclude_reasons": {}
        },
        {
          "from": "guides",
          "to": "guides",
          "kind": "support",
          "exclude_reasons": {}
        },
        {
          "from": "templates",
          "to": "templates",
          "kind": "support",
          "exclude_reasons": {}
        }
      ],
      "mandatory": [],
      "limitations": [
        "The upstream skills reference `scripts/export-documents.mjs`, `scripts/scaffold-state.mjs` and `scripts/profile-strength.mjs`, a Node 22 plus Typst toolchain with a checked-in dependency bundle. That toolchain is deliberately not vendored: it is an application, not a prompt library, and this repository has no Node CI to keep it honest. Everything the skills do with Markdown works without it. For DOCX and PDF export, clone the upstream repository and run it there.",
        "The skills expect a `my-documents/` state layer in the user's own working directory, described in `skills/_shared/state-layer.md`. That is the user's data and is not vendored. Run the `get-started` skill, or upstream's `npm run scaffold`, to create it.",
        "`skills/_shared/` holds shared instructions rather than a skill, so it carries no SKILL.md and is exempt from the per-skill provenance rule."
      ],
      "notes": "The upstream skills carry 47 links to ../_shared/state-layer.md and further links to ../../guides/ and templates/. Copying only skills/ would leave every one of those links pointing at nothing, so guides/ and templates/ are mapped alongside at the same relative distance and the links resolve unchanged. This is why a manifest source declares a list of paths rather than one subdirectory."
    }
  ]
}
